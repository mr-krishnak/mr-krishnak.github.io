## Hi
My portfolio

### Maintenance
- Fixed npm audit issues and file ownership (chown) on `node_modules` and npm cache.
- Added dev dependency `typescript` to satisfy ESLint.
- Upgraded `eslint` to v9 to match `eslint-config-next`.
